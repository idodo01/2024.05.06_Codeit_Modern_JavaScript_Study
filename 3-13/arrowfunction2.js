// Arrow function: 화살표 함수

// 파라미터가 하나일 경우,
// () 생략 가능 
// but, () 권장
const getTwice = number => {
  return number * 2;
};

// 파라미터가 둘 이상일 경우,
// () 필수
const sum = (a,b) => {
  return a + b;
};
